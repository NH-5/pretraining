# 代码审查报告（复核修订版）

> **原报告日期**：2026-07-31
> **复核日期**：2026-07-31
> **修复日期**：2026-07-31
> **范围**：仓库全部 Python 代码，修复后共 5319 行；`exercises/` 下 ex00–ex10 及判题基础设施
> **复核目标**：逐项验证原报告 19 项发现及“经实测为健全”的结论
> **方法**：静态追踪代码/契约、进程内正反例注入、两进程 Gloo 实测、Ex1 因果/双向各 3000 步实训，并对照《大模型预训练入门指南》与 Hugging Face `tokenizers` 官方 API
> **修复边界**：已修改公开检查、非核心脚手架、提示与文档；没有填入 ex03–ex10 的学习者核心 TODO，没有推进 `PROGRESS.md`，也没有修改依赖声明或 lockfile

---

> **阅读提示**：第 2 节及第 3.2–3.5 节保留的是**修复前复核证据**，用于说明原报告为什么成立或需要修正；其中“当前检查”和行号均指修复前快照。修复后的现状以第 1.2、3.6、5 节为准。

## 1. 复核结论

原报告发现了多项真实的公开检查盲区，但部分复现实证、严重度和修复建议不准确。逐项复核结果为：

- **13 项核心结论准确**；
- **5 项主问题存在，但复现、归因或严重度需要修正**；
- **1 项（原 M3）整体表述错误，仅有一个较窄的子问题成立**。

需要特别区分两种目标：

1. 这个仓库是学习练习，不是对抗性在线判题系统；公开检查不能也不应替代 README 中的训练数字、口述和人工抽查。
2. 但对“形状正确、核心数学错误”的常见实现，公开检查仍应尽量给出可靠反馈。

因此，只有能让**自然出现的错误实现**误过核心学习目标的问题列为高/中优先级；依赖硬编码公开锚点、故意修改非 TODO 脚手架或忽略 MANUAL 语义的情形不再统一视为“高危”。

### 1.1 修订后的发现总览

| ID | 修订判定 | 修订严重度 | 一句话结论 |
|---|---|---:|---|
| H1 | 部分准确 | 🟠 中 | attention 路径缺少端到端因果检查；但原报告的具体注入与“生成也能验收”不成立 |
| H2 | 准确 | 🔴 高 | BPE 检查只验证可逆性，不验证是否真的学习了 merge |
| H3 | 部分准确 | 🟠 中 | 默认 ByteLevel 配置存在提示缺口，但不能称为“正确实现被误杀”，`<unk>` 也非必需 |
| H4 | 准确 | 🔴 高 | 单卡等价性完全相信学习者返回的标量，恒返回 0 可 PASS |
| H5 | 准确 | 🔴 高 | eval 未独立验证多 batch 均值，也没有带 dropout 的模式探针 |
| H6 | 准确 | 🔴 高 | SwiGLU/GQA/RoPE 的核心数学均可被“形状正确、机制错误”的实现绕过 |
| M1 | 准确但漏项 | 🟠 中 | sum reduction 在 ex01、ex04、ex09 均可 PASS |
| M2 | 准确 | 🟠 中 | 线性衰减满足当前 warmup-cosine 检查 |
| M3 | 原结论错误 | 🟡 低 | MFU 已被主 `check` 验证；只有 `self-test` 子命令漏测 MFU |
| M4 | 技术事实准确 | 🟡 注记 | fuzzy no-op 会得到 MANUAL，但该项本来就是可选人工验收 |
| M5 | 技术事实准确 | 🟡 低 | “unresolved” 实际统计 TODO 标记；工具文档已明确它不是学习进度判题器 |
| M6 | 准确 | 🟠 中 | manual all-reduce 只测一个参数张量，且没有 manual 端到端等价检查 |
| M7 | 准确 | 🟠 中 | 相同 seed 不等于共享初始化，组件对照存在额外随机变量 |
| M8 | 准确 | 🟠 中 | 生成探针只依赖最后一个 token，无法验证上下文窗口是否真的被使用 |
| L1 | 部分准确 | 🟡 低 | 只测 T=4 且拒绝合法广播形状；但 HINTS 并未明确推荐四维 mask |
| L2 | 准确 | 🟡 低 | `generate()` 不恢复进入函数前的训练模式 |
| L3 | 准确 | 🟡 低 | fuzzy 删除数使用审计 pair 数，可能重复计数 |
| L4 | 准确 | 🟡 低 | 返回伪造的梯度范数仍可通过 |
| L5 | 准确 | 🟡 注记 | PENDING/MANUAL exit 0 是文档化设计，但完成门槛措辞仍可更明确 |

### 1.2 当前修复状态

原报告的 19 项均已处理。M4、L5 涉及的渐进式学习语义没有被粗暴改成“所有非 PASS 都 exit 1”，而是在保留 `SKIP/MANUAL/PENDING` 设计的前提下补强完成门槛和错误实现识别。

| 范围 | 对应发现 | 当前状态 |
|---|---|---|
| Ex1 因果性 | H1、L1 | T=4/T=7、合法广播形状和完整模型 future-invariance 均已检查 |
| Ex1 生成 | M8、L2 | 探针依赖完整滚动窗口；检查上下文长度、温度边界和 train/eval 模式恢复 |
| 逐 token loss | M1 | ex01、ex04、ex09 均以均匀 logits 的 `ln(V)` 锚拒绝 sum reduction |
| Ex3 BPE | H2、H3 | 检查 BPE model、非空 merges、完整 byte coverage 和实际压缩；补充 ByteLevel、alphabet、`<unk>` 版本边界提示 |
| Ex4 eval | H5 | 使用含 dropout 的探针，并独立计算 3-batch eval-mode 平均值 |
| Ex5 稳定性 | M2、L4 | cosine 在 25%/75% 处做数值锚；报告 grad norm 与参考裁剪前 norm 对照 |
| Ex6 公式 | M3 | `self-test` 现在复用 MFU 正常值与 >100% 拒绝检查 |
| Ex7 数据 | M4、L3 | 已实现的 fuzzy no-op 会 FAIL；删除数改由输入/输出文档数之差计算 |
| Ex8 分布式 | H4、M6 | 多参数、不同 shape、`grad=None`、manual global-batch 更新均检查；末参数扰动可识别常量/只看首参的等价函数 |
| Ex9 组件 | H6、M7 | Q/K RoPE、SwiGLU 数值门控、GQA KV 投影结构均补强；同名同 shape 参数从同一 baseline state 复制 |
| 总检查器与文档 | M5、L5 | 输出改为 `TODO markers present`；Ex9 明确“必做项与所选组件必须 PASS” |

---

## 2. 详细发现（修复前复核证据）

### 🔴 高优先级

#### H2 — ex03：BPE merge 正确性未被验证

- **位置**：`exercises/ex03_bpe_tokenizer/train.py:104-158`
- **指南目标**：指南 §5.2 要求理解 BPE“反复合并最高频相邻对”，§5.3 要观察词表/序列长度权衡。
- **当前检查**只验证：
  - 保存文件存在；
  - `<bos>/<eos>/<pad>` 存在；
  - 词表不超过请求值；
  - 3 条 UTF-8 文本可往返。
- **复核实证**：用一个完全没有 merge 概念、只做 UTF-8 字节可逆编解码的测试替身，仍得到：

  ```text
  saved tokenizer.json; special tokens present; 3 UTF-8 round trips passed
  ```

- **影响**：学习者可以完成“可逆字节编码”，却没有练到 BPE merge 与序列压缩。
- **建议**：
  1. 使用有确定高频 pair 的极小语料；
  2. 解析保存的 `tokenizer.json`，检查 `model.merges` 非空并包含预期早期 merge；
  3. 对训练语料断言 BPE token 数严格少于纯字节 token 数；
  4. 不要只用最终 `vocab_size` 推断 merge 正确性。

#### H4 — ex08：单卡等价性检查以学习者函数为 oracle

- **位置**：`exercises/ex08_distributed/train.py:284-320`
- **问题**：判题器只检查 `verify_against_single_card()` 返回有限且不超过 `1e-6`，没有验证该值是否真的来自单卡参考训练。
- **两进程实证**：

  ```text
  verify_against_single_card = lambda **kwargs: 0.0
  [PASS] EX08_SINGLE_CARD_EQUIVALENCE
  Summary: 4 passed, 0 failed, 1 skipped
  ```

- **影响**：核心等价性 TODO 可以恒返回 0。
- **建议**：加入变形测试。先对正确分布式模型调用一次，再人为扰动一个最终参数并再次调用；若函数仍返回 0，则 FAIL。这样既不必把完整参考实现直接写给学习者，又能抓住常量返回值。

#### H5 — ex04：eval 检查不验证多 batch 均值和 dropout 模式

- **位置**：`exercises/ex04_eval_checkpoint/train.py:151-183`
- **实证**：
  - 循环覆盖 loss、只返回最后一个 batch：PASS；
  - 从不调用 `model.eval()`、只计算平均：PASS。
- **原因**：
  - 检查没有独立计算 3 个 batch 的期望均值；
  - `TinyNextTokenModel` 不含 dropout，因此 train/eval 数值行为相同。
- **建议**：
  1. 判题器独立计算各 batch loss 的平均；
  2. eval 探针模型加入非零 dropout；
  3. 保留现有“恢复进入函数前模式”和“参数不变”检查。

#### H6 — ex09：组件检查没有验证核心数学

- **位置**：
  - RoPE：`exercises/ex09_modern_component/train.py:163-196`
  - SwiGLU：`:199-211`
  - GQA：`:214-251`
- **实证**：
  - `down(gate + up)` 的相加版“伪 SwiGLU”：PASS；
  - 只旋转 Q、原样返回 K 的 RoPE：PASS；
  - 忽略 `num_kv_heads`、使用普通 MHA：PASS。
- **建议**：
  - **SwiGLU**：固定权重与输入，和独立的 `down(silu(gate) * up)` 数值参考对比；
  - **RoPE**：分别断言 Q/K 的非零位置确实改变，并检查二者使用相同角度；
  - **GQA**：检查 K/V 投影权重形状及 KV head 数，并加入共享关系的确定性数值探针。

---

### 🟠 中优先级

#### H1 — ex01：attention 路径缺少端到端因果性检查

- **位置**：
  - 独立 mask 检查：`exercises/ex01_char_gpt/train.py:226-247`
  - attention 使用处：`:75-98`
- **主问题成立**：`_check_causal_mask()` 只验证 `build_causal_mask()` 的返回矩阵，没有验证完整 attention 输出对未来 token 不敏感。
- **原报告中两处实证不准确**：
  1. 直接把 `build_causal_mask()` 改成全 True 时，`EX01_CAUSAL_MASK` 会 FAIL，不是三项全 PASS；
  2. 必须保留正确的 mask 函数、另行移除 `CausalSelfAttention.forward()` 中的 mask 使用，才会出现三项检查全部 PASS。
- **端到端复核**：移除 attention 中的 mask 后：

  ```text
  [PASS] EX01_CAUSAL_MASK
  [PASS] EX01_TOKEN_LOSS
  [PASS] EX01_AUTOREGRESSIVE_GENERATION
  future_change_earlier_logit_max_diff=0.0118973
  ```

  这证明更改未来 token 已经影响更早位置的 logits，但公开检查没有发现。

- **3000 步对照**：

  | 路径 | step 3000 loss | 生成结果 |
  |---|---:|---|
  | 正常因果 attention | `1.6430` | 有莎翁式姓名、换行和句式 |
  | 双向 attention | `0.0241` | 约 300 个连续 `F` |

  双向模型确实可以靠偷看未来轻易通过 loss 门槛，但原报告声称“生成同样像莎翁、完整验收也能满足”不成立。README 的人工样本验收能抓住这一问题。

- **建议**：增加无需训练的完整模型因果不变量测试：
  1. 关闭 dropout；
  2. 构造两个前缀相同、未来部分不同的输入；
  3. 断言前缀位置 logits 完全不变。

  注意：把生成探针改成非马尔可夫模型只能修 M8，不能替代这条 attention 因果性测试。

#### H3 — ex03：ByteLevel 默认参数与提示之间存在缺口

- **位置**：`exercises/ex03_bpe_tokenizer/HINTS.md:49-101`
- **官方 API 事实**：
  - `pre_tokenizers.ByteLevel()` 默认 `add_prefix_space=True`；
  - `ByteLevel.alphabet()` 返回完整 256 字节字母表；
  - `BpeTrainer.initial_alphabet` 用于包含训练语料中未出现的字节表示。
- **需要修正原报告的地方**：
  1. HINTS 只列出组件和 API 名，并要求查看当前版本；它没有明确要求使用错误的默认参数组合；
  2. 完整 ByteLevel 字母表天然可覆盖任意字节，`models.BPE()` 使用 `unk_token=None` 时不要求注册 `<unk>`；
  3. 只有显式配置 `unk_token="<unk>"` 时，才必须保证该 token 进入词表。
- **正确结论**：这是提示完整性/版本稳定性问题，不足以称为“正确实现被误杀”。
- **建议**：
  - HINTS 明确提示 exact round-trip 场景使用 `add_prefix_space=False`；
  - 明确写出 `initial_alphabet=ByteLevel.alphabet()`；
  - 说明 `<unk>` 的两种合法选择，而不是无条件要求注册；
  - 到达 Ex3 后按仓库约定用 `uv add tokenizers` 锁定实际版本。
- **参考**：
  - <https://huggingface.co/docs/tokenizers/main/api/pre-tokenizers>
  - <https://github.com/huggingface/tokenizers/blob/v0.23.1/bindings/python/py_src/tokenizers/pre_tokenizers.pyi>

#### M1 — ex01/ex04/ex09：loss 检查不验证 mean reduction

- **位置**：
  - ex01：`exercises/ex01_char_gpt/train.py:250-288`
  - ex04：`exercises/ex04_eval_checkpoint/train.py:124-148`
  - ex09：`exercises/ex09_modern_component/train.py:141-160`
- **实证**：三处把交叉熵改为 `reduction="sum"` 后均 PASS。
- **影响**：训练日志失去“平均每 token CE”的语义，ex01 默认 `B×T=4096` 时 loss 被放大 4096 倍。
- **修正**：原报告只列 ex01/ex04，漏掉了使用同类检查的 ex09。
- **建议**：使用零 logits 数值锚，断言 loss 等于 `ln(V)`；这能直接区分 mean 与 sum。

#### M2 — ex05：线性衰减也通过 warmup-cosine 检查

- **位置**：`exercises/ex05_training_stability/train.py:159-219`
- **实证**：

  ```text
  linear warmup + linear decay
  lr[0]=0, peak=0.01, lr[20]=0.001
  PASS
  ```

- **建议**：除端点和单调性外，在 decay 进度 25% 和 75% 处与余弦公式做数值对照；50% 处线性与余弦恰好重合，不能用于区分。

#### M6 — ex08：manual all-reduce 只测试单个参数张量

- **位置**：`exercises/ex08_distributed/train.py:258-281`
- **实证**：只同步模型中第一个非空梯度参数，配合 H4 的常量等价性返回值，仍得到：

  ```text
  4 passed, 0 failed, 1 skipped
  ```

- **建议**：
  1. `_check_all_reduce` 使用至少 2 个不同形状的参数；
  2. 包含一个 `grad is None` 参数；
  3. 增加 manual 策略与单卡 global batch 的端到端参数比较。

#### M7 — ex09：相同 seed 没有保证共享初始化

- **位置**：`exercises/ex09_modern_component/train.py:67-92`、`:268-281`
- **实证（seed=29，baseline vs RoPE）**：

  ```text
  token_embedding:       equal
  final_norm.weight:     equal
  block0 qkv.weight:     different
  block0 FFN weight:     different
  ```

- **影响**：单次 loss 差异同时包含组件效果和共享层随机初始化差异。
- **建议**：
  - 将两模型中同名且同 shape 的参数显式复制；
  - 对无法共享的组件单独初始化；
  - 至少运行多个 seed 并报告均值/方差，不能把单次结果解释成组件普遍结论。

#### M8 — ex01：生成探针没有验证上下文窗口

- **位置**：`exercises/ex01_char_gpt/train.py:291-385`
- **问题**：确定性模型的下一 token 只依赖当前 token。
- **实证**：每轮只把 `prompt[:, -1:]` 喂给模型的实现仍通过序列与温度检查。
- **建议**：测试替身的下一 token 应依赖整个输入窗口，例如由窗口 token 和或首尾组合决定；同时记录每轮实际传入模型的上下文长度。

---

### 🟡 低优先级 / 设计注记

#### M3 — ex06：只有 `self-test` 漏测 MFU，主检查已经覆盖

- **原报告标题“MFU 从未被测”不成立。**
- **现有主检查**：`exercises/ex06_scaling_math/train.py:120-143` 已验证：
  - `N=1B, 100k token/s, 1000 TFLOP/s → MFU=0.6`；
  - 超过 100% 的输入必须抛错。
- **实证**：把 `estimate_mfu` 硬编码为 `0.6` 时：

  ```text
  self-test: PASS
  EX06_MFU: FAIL
  An impossible MFU input must raise instead of returning 0.6.
  ```

- **剩余问题**：`run_self_test()` 没调用 MFU，而 README 把 `self-test` 列为第一项验收。
- **建议**：让 `self-test` 调用和主 `check` 相同的 MFU 锚，或明确 `check` 才是四个公式的权威入口。

#### M4 — ex07：fuzzy no-op 会得到 MANUAL

- **位置**：`exercises/ex07_data_pipeline/train.py:181-206`
- **实证**：

  ```text
  fuzzy_deduplicate = lambda records: (records, [])
  MANUAL: kept=4, audit_pairs=0
  ```

- **定性修正**：
  - fuzzy dedup 明确是可选拓展；
  - MANUAL 不是 PASS；
  - 任意输入上 audit 为空不必然是错误，阈值可能合理地判断“无近重复”。
- **建议**：在判题器专门构造一组必然超过约定阈值的重复样本并要求产生审计对；真实数据上的“至少 3 对”继续作为人工验收，不要把所有空审计无条件判 FAIL。

#### M5 — `check_scaffolds.py` 的 “unresolved” 命名不准确

- **位置**：`exercises/check_scaffolds.py:176-193`
- **实证**：ex01 三个行为检查已 PASS，但因 TODO 注释保留，结构检查仍显示：

  ```text
  ex01_char_gpt: unresolved learning targets 3/3
  total: 36/36
  ```

- **定性修正**：`exercises/README.md:58-65` 已明确 `--expect-unfinished` 只用于最初空白脚手架，不是学习进度判题器。
- **建议**：将输出改名为 `TODO markers present`；真正进度继续由当前练习 `check`、README 验收和 `PROGRESS.md` 共同决定。

#### L1 — ex01 mask 只测 T=4，shape 契约不一致

- **位置**：`exercises/ex01_char_gpt/train.py:226-247`
- **问题**：
  - 只测 T=4；
  - 函数 docstring 要求“可广播到 `[B,H,T,T]`”，但检查严格要求 `(T,T)`。
- **修正原报告措辞**：HINTS §41 说明的是 `[T,T]` 如何广播，并未明确推荐 `[1,1,T,T]`；接受四维形状的依据是函数 docstring。
- **建议**：测试 T=4/T=7，并接受能安全广播到 attention scores 的 bool mask。

#### L2 — ex01 `generate()` 不恢复模型模式

- **位置**：`exercises/ex01_char_gpt/train.py:188-219`
- **实证**：调用前 `model.training=True`，返回后为 False。
- **建议**：保存进入函数前的模式，在 `finally` 中恢复；`@torch.no_grad()` 不能替代 train/eval 状态管理。

#### L3 — ex07 fuzzy 删除数可能重复计数

- **位置**：`exercises/ex07_data_pipeline/train.py:101-104`
- **问题**：`fuzzy_removed = len(fuzzy_audit_records)` 把“审计 pair 数”等同于“删除文档数”。
- **建议**：优先使用 `len(exact_kept) - len(output_records)`；如果需要从审计计算，则按被删文档稳定 ID 去重。

#### L4 — ex05 不验证返回的梯度范数

- **位置**：`exercises/ex05_training_stability/train.py:260-289`
- **实证**：正确执行更新但恒返回 `reported_norm=0.0`，检查仍 PASS。
- **建议**：把学习者报告值与参考路径中 `clip_grad_norm_` 返回的裁剪前 norm 对照。

#### L5 — PENDING/MANUAL exit 0 是设计，但完成门槛应更醒目

- **位置**：`exercises/checking.py:42-95`
- **结论**：只有 FAIL 导致非零退出是文档化行为，适合可渐进填写的学习脚手架。
- **风险**：ex09 “只要所选项不是 FAIL 即可继续”的措辞可能让 PENDING 被误读成完成。
- **建议**：
  - README 明确写出“必做项必须 PASS；MANUAL 必须完成人工复核；PENDING 表示未完成”；
  - 如需自动化完成门，可另加显式 `--require-complete`，不要改变默认渐进式检查行为。

---

## 3. 动态复核结果（修复前后）

### 3.1 全量结构与安全入口

```bash
uv run python exercises/check_scaffolds.py --structure-only
uv run python exercises/check_scaffolds.py
```

两条命令均 exit 0；后者会运行每个安全检查入口，但当前大部分核心 TODO 仍是 PENDING。结构检查报告 11 个练习通过、TODO 标记 `36/36`。

### 3.2 Ex1 正常因果模型

```bash
uv run python exercises/ex01_char_gpt/train.py train --max-steps 3000
```

真实结果：

```text
device=mps
step=0001 train_loss=4.2070
step=1000 train_loss=2.1744
step=2000 train_loss=1.8275
step=3000 train_loss=1.6430
```

- 总耗时约 270 秒（约 4.5 分钟，平均约 90 ms/step，包含启动和首次编译开销）；
- 日志总体下降但并非严格单调，例如：
  - step 300：`2.4503`
  - step 400：`2.4590`
  - step 2400：`1.6949`
  - step 2500：`1.8041`
- 无明显 spike/NaN；
- 样本已出现莎翁式姓名、换行与句式；
- 默认 3000 步没有达到 README 的 `train loss < 1.5`。

因此，原报告“65 ms/step、3000 步约 3.2 分钟、600 步曲线单调”的表述不应保留为当前已验证事实。

### 3.3 Ex1 双向 attention

保持其他配置与 seed 不变，只移除 attention 中的 causal mask：

```text
step=0001 train_loss=4.1901
step=1000 train_loss=2.3389
step=1500 train_loss=0.0458
step=3000 train_loss=0.0241
sample=FFFFFFFFFFFFFFFF...
```

结论：未来泄漏能制造极低训练 loss，但生成发生灾难性分布偏移；loss 与生成质量必须同时验收。

### 3.4 Ex8 两进程 Gloo

正确参考实现：

```text
[PASS] EX08_ALL_REDUCE
[PASS] EX08_SINGLE_CARD_EQUIVALENCE
reference max_abs_diff=1.490e-08
```

错误注入（只同步第一个参数、等价函数恒返回 0）：

```text
4 passed, 0 failed, 1 skipped
```

这同时确认 H4、M6 成立，也复现了原报告给出的正确实现差值。

### 3.5 Ex4 checkpoint

在进程内提供正确的 TODO 实现后：

```text
model/optimizer/scheduler/step/metadata/CPU RNG round trip passed
resume/reference max diff: 0.000e+00
```

需要区分：

- 普通 `check` 验证保存/扰乱/加载后的状态完全恢复；
- “继续训练 vs 不间断训练”的逐步连续性比较位于 `demo` 路径。

原报告把两者都称为“checkpoint round-trip 判题器”略不精确，但两条验证逻辑本身健全。

### 3.6 修复后回归

最终静态与全量安全入口：

```text
python -m compileall -q exercises   PASS
git diff --check                    PASS
11/11 exercise scaffolds            PASS
TODO markers present                36/36
```

Ex1 当前实现：

```text
EX01_CAUSAL_MASK                 PASS
EX01_TOKEN_LOSS                 PASS  (uniform V=5 -> 1.609438)
EX01_AUTOREGRESSIVE_GENERATION  PASS  (context lengths 2,3,4,4,4)
```

进程内正反例回归结果：

| 检查 | 正例 | 修复前可绕过的反例 |
|---|---|---|
| Ex1 attention/loss/generation | PASS | 忽略 mask、sum loss、last-token-only generation 均被拒绝 |
| Ex3 BPE | — | 零 merge 可逆 byte codec 被拒绝 |
| Ex4 eval/loss | PASS | last-batch-only eval、sum loss 被拒绝 |
| Ex5 schedule/accumulation | PASS | 线性 decay、伪造 `grad_norm=0` 被拒绝 |
| Ex6 self-test | PASS | 恒返回 `MFU=0.6` 被拒绝 |
| Ex7 fuzzy/statistics | PASS | fuzzy no-op 被拒绝；重复 audit pair 不再重复计数 |
| Ex9 RoPE/SwiGLU/GQA/loss | PASS | Q-only RoPE、加法 SwiGLU、普通 MHA、sum loss 均被拒绝 |

Ex8 两进程 CPU/Gloo 修复后实测：

```text
all parameter gradients averaged; grad=None preserved
manual update max_abs_diff=0.000e+00
clean single-card max_abs_diff=1.490e-08
last-parameter corruption detected=1.000e-02
first-parameter-only all-reduce: rejected on both ranks
constant-zero equivalence:       rejected on both ranks
```

这些正例实现只在临时测试进程中注入，没有写入仍属学习任务的核心 TODO。

---

## 4. 经复核且修复后仍健全的部分

- **ex00 环境检查**：当前实测 PyTorch `2.13.0`、CUDA=False、MPS=True、Apple M1 / 16 GB 统一内存、推荐 fp32、张量 smoke PASS。
- **ex01 当前学习者实现**：
  - causal mask 含对角线且无未来泄漏；
  - loss 使用全部 token 且为 mean reduction；
  - generation 正确使用滚动上下文、裁剪窗口、采样并恢复模型模式；
  - tied embedding/head 经 PyTorch 参数去重后共有 52 个唯一参数张量，不存在双更新。
- **ex02 trace wiring**：前缀长度与右移目标对齐正确；书面答案继续使用人工口述验收。
- **ex04 checkpoint 状态往返与 demo 连续性**：逻辑健全，但应准确区分两个入口。
- **ex05 梯度累积核心对照**：与 global batch 参数更新最大差在 `1e-6` 容差内；bf16 只在支持的 CUDA 上启用。
- **ex06 公式与单位目标**：`C≈6ND`、20 token/param、TFLOP/s 换算和 MFU 的主检查/self-test 现均覆盖正常值与非法边界。
- **ex07 normalization、audit sampler 与 exact dedup 检查**：NFC/空白规范化、确定性抽样和首次出现保序契约正确；fuzzy 仍保留最终人工阈值复核。
- **ex08 正确实现**：本机两进程 Gloo 路径复现 `1.490e-08`，manual 更新差异为 0；这仍只是 CPU 控制流 smoke test，不能冒充 2+ CUDA/FSDP 正式验收。
- **ex09 检查**：causal boundary、mean token loss、RoPE、SwiGLU、GQA 的正确临时实现均通过，修复前记录的四类错误实现均被拒绝。
- **ex10 response mask 与 response-only loss 检查**：
  - prompt 梯度为 0；
  - response 位置均有梯度；
  - 修改被 mask logits 不改变 loss；
  - 零 logits 数值锚为 `ln(4)`，能拒绝 sum reduction。

---

## 5. 修复实施结果

| 原优先级 | 问题 | 实施结果 |
|---:|---|---|
| 1 | H5 eval 语义 | ✅ 独立平均参考 + 含 dropout 的探针 |
| 2 | H6 组件数学 | ✅ SwiGLU 数值锚、Q/K RoPE、GQA KV-head 结构 |
| 3 | H4 单卡等价性自证 | ✅ clean/corrupted 两次调用，末参数 +0.01 必须被报告 |
| 4 | H2 BPE merge | ✅ 检查 BPE JSON、merges、byte coverage 和语料探针压缩 |
| 5 | H1 / M8 | ✅ 完整模型因果不变量 + 依赖整个窗口的 generation 探针 |
| 6 | M1 / M2 / M6 | ✅ mean-loss、cosine 数值点、多参数/manual all-reduce |
| 7 | M7 / H3 | ✅ 共享兼容初始化 + ByteLevel 参数说明 |
| 8 | M3 / M5 / L1–L5 | ✅ self-test、统计口径、命名、模式恢复和完成门槛均处理 |

---

## 6. 复核限制

- 仓库当前未安装 `tokenizers`，且项目约定安装新依赖前需确认，因此没有修改 `pyproject.toml` 或 `uv.lock`。修复后的 Ex3 已验证零 merge 反例会被拒绝，但真实 Hugging Face tokenizer 正例需在用户到达 Ex3、确认安装依赖后再运行；H3 的默认参数结论来自 0.23.1 官方类型定义和官方文档。
- 本机没有 CUDA；FSDP、NCCL、bf16 显存与真实多卡吞吐未做本地验证。Ex8 只运行了两进程 CPU/Gloo。
- 所有正反例注入都在进程内完成；未把参考答案或错误实现写入练习源码。
