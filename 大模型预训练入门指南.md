# 大模型预训练入门指南

> 给"已经懂深度学习、但没接触过预训练"的读者。先建立全局认知,再下钻每个环节,最后给出可上手的实践路径。
> 全文可分章读,顺序即推荐学习顺序。带 ⚑ 的是关键直觉,带 ⌨ 的是可动手验证的点,带 ⚠ 的是常见踩坑。

---

## 0. 这份指南怎么用

你已经会训练一个小分类器、会反传、会看 loss 曲线。这份指南要做的是把"从零训练一个 billion 级语言模型"这件事的**全貌**补上:它和你熟悉的监督学习差在哪、多了什么工程难题、为什么烧一个模型动辄上万张卡。

阅读策略:

1. 先读 **1、2** 拿到全局图,知道每块拼图在哪。
2. 按 **3 → 12** 下钻,每章做完"自测题"再往下。
3. **13、14** 是实践入口,边读边跑一个小 demo(单卡就能跑)。
4. 术语表和资源清单放最后,随时查。

---

## 1. 什么是"预训练",它和微调有什么区别

一句话:**预训练 = 在海量无标注文本上,用自监督目标训练一个通用语言模型。** "Pre" 是相对"后续的任务训练"而言的——先把通用的语言能力训练出来,再针对具体任务做后训练(post-training)。

为什么这样做有效?

- **数据是无标注的**:整个互联网的文本都能用,量级是 `10^12`~`10^13` token,而有标注数据顶多 `10^9` 量级。无监督信号从数据本身出,不需要人工标注。
- **学的是表示 + 世界知识 + 接下来该说什么**:训练目标看似简单(预测下一个 token),但为了预测得准,模型被迫学会语法、事实、推理、代码、翻译…… 这些能力**涌现**在大规模 + 大数据 + 大算力三者叠加之下。
- **一个模型干所有任务**:同样的权重,后训练后能做翻译、摘要、问答、写代码——这是"预训练 + 后训练"范式相对"每个任务训一个 BERT/RNN"的范式跃迁。

```
          ┌───────────── 预训练 (Pretraining, 本指南主角) ─────────────┐
原始文本 →  清洗/去重/分词 → 自回归预测下一个token → 得到 "base model" (底模)
          └─────── 这一步烧掉 99% 的算力,决定了模型的"能力上限" ──────┘
                                  ▼
          ┌──────────── 后训练 (Post-training / Alignment) ───────────┐
底模  →  SFT监督微调 → 强化学习对齐(RLHF/DPO/GRPO) → 会聊天/会遵循指令的 Chat 模型
          └─────── 把"能续写文本的猛兽"驯成"好用的助手",本指南第12章概览 ───┘
```

⚑ **关键直觉**:预训练决定模型的"智力天花板",后训练决定它"表现得多像好助手"。你看到的 ChatGPT/Qwen 的对话能力,底层 99% 来自预训练阶段;后训练是塑造,不是注入智力。

---

## 2. 全局地图:预训练的五大环节

把预训练拆成五块,后面每章对应一块。先把这张图记住,细节都是往里填的:

| 环节 | 回答的问题 | 对应章节 |
|---|---|---|
| **数据 Data** | 拿什么文本训?怎么清洗、去重、配比?质量怎么保证? | 4 |
| **分词 Tokenization** | 文本怎么变成模型能吃的整数序列?词表多大? | 5 |
| **架构 & 目标 Model & Objective** | 用什么网络?训练目标函数是什么? | 3、6 |
| **训练 Training** | 怎么把上面这些跑起来?优化器/学习率/精度/并行? | 7、8、9 |
| **评估 & 稳定性 Eval & Stability** | 训得对不对?loss 为什么 spike? | 10 |

还有一个横切的"**Scaling Law**"(第 8 章):给定预算,把模型做多大、数据训多少才划算——它是这五块的指挥棒。

---

## 3. 必备回顾:只覆盖和预训练强相关、你之前可能没细想的部分

既然你懂深度学习,这里只补"预训练里那些和普通监督学习不一样"的点。熟悉的可跳。

### 3.1 Decoder-only(因果/自回归)架构是主流

现代大模型(GPT 系、LLaMA、Qwen、DeepSeek、Mistral)几乎都是 **decoder-only Transformer**:一个纯由"因果注意力 + FFN"堆叠的塔,没有任何 encoder。

- **因果掩码 causal mask**:第 `t` 个位置只能看到 `1..t`,看不到未来。这一条让"预测下一个 token"成立。
- 为什么不用 BERT 的双向 encoder?因为生成式任务(续写)天然是因果的;而且实践上发现 decoder-only 在 scale 上来后,双向 MLM 的优势就没了,反而在生成/对话上更统一。

### 3.2 自回归 = 一直在解一个多分类

每个时刻模型输出一个对**整个词表 V** 的概率分布,目标是让真实下一个 token 的概率尽量大。所以预训练本质上是个 **V 分类问题**,V 通常是 3万~20万量级。

### 3.3 主流架构的小约定(和教科书 Transformer 的差异)

教科书的 `Attention Is All You Need` encoder-decoder + 正弦位置编码,现代 decoder 模型做了不少替换,见到的论文里基本都是下面这套:

| 位置 | 约定 | 好处 |
|---|---|---|
| Norm 位置 | **Pre-Norm**(残差前归一化,常见 RMSNorm + Llama 风格) | 深层稳定,可堆到上百层不发散 |
| 位置编码 | **RoPE**(旋转位置编码)或 ALiBi | 外推到更长上下文,不需重训 |
| FFN | **SwiGLU**(门控 + GLU 激活) | 比标准 FFN 表现更好 |
| 注意力 | **GQA / MQA**(分组/多查询注意力) | KV cache 更小,QK-norm 加稳 |
| Embedding | 输入嵌入与输出 unembedding **weight tying** | 省参数、有时涨点 |
| 归一化 | **RMSNorm** 替代 LayerNorm | 省去 mean 的计算,更稳更省 |

⚠ 新手误区:别拿 2017 版的 Transformer 直接复刻去训 7B,大概率训不稳。直接照 LLaMA 的配方走。

---

## 4. 数据:预训练里"最难被复制"的一环

如果你只能深入一个环节,深入数据。原因:**架构是公开的,算力是买得到的,数据是护城河。** 同样的钱,数据质量差一个数量级,模型就差一个档次。数据决定了模型的上限。

### 4.1 数据从哪来(语料构成)

主流预训练语料是**网页抓取 + 精选高质量文本**的混合,大致构成:

- **网页抓取**:Common Crawl 衍生数据集(RedPajama、FineWeb、RefinedWeb、DCLM 等),量级最大,占总 token 的大头。
- **精选高质量**:维基百科、书籍(Gutenberg / Books3)、arXiv 论文。
- **代码**:GitHub / The Stack——让模型获得"执行/推理"能力。
- **数学**:用得上的数学语料相对稀缺,所以很多模型会**合成**数学数据。
- **多语言 / 领域**:为多语言能力配比各语种。

### 4.2 数据处理流水线(从原始 HTML 到训练 token)

```
原始网页 → 提取正文 → 语言识别/过滤 → 去重 → 质量过滤 → 配比混合 → 包装成训练样本 → 分词
```

每一步都值得单独学,但关键直觉:

1. **正文抽取**:HTML 里全是导航栏/广告/boilerplate,要提取"内容主体"。这一步丢错会污染。
2. **质量过滤**:用启发式(长度、重复率、困惑度/语言模型打分、badword)和分类器把低质页面剔掉。FineWeb 用了基于 Llama-3 的质量分类器。Phi 系列干脆主打"高质量 + 合成数据"(`Textbooks Are All You Need`)。
3. **去重 dedup**:同一篇文章在全网重复几十次很常见。去重分 **exact dedup**(精确)和 **fuzzy dedup**(MinHash/LSH 近似)。去重是提质量最重要的一步,直接影响泛化。
4. **配比 weighting**:网页占比 60%+ 不可少(覆盖广),但代码/数学占比提高会明显提升推理能力。配比是反复 A/B 出来的经验值。

⚑ **直觉**:同样的数据集,做好去重和过滤后,训出的模型能比"原始 dump"好一截——且省算力。

### 4.3 要训多少 token?(数量维度)

Chinchilla 给的经验值:**compute-optimal 下,约 20 个 token / 每个参数**。但近年出现"过训练(overtraining)"趋势:**为了推理便宜,把小参数模型训更久**(见第 8 章详解)。

- 例如 LLaMA-3 8B 模型训了 ~15T token,远超 Chinchilla 最优的 ~160B(8B × 20)。
- DeepSeek / Qwen 这种后起之秀也普遍过训练。

⚖ 一个对比例子:

| 模型 | 参数 | 训练 token | 是否 Chinchilla 最优 |
|---|---|---|---|
| Chinchilla | 70B | 1.4T | 是(约 20 tok/param) |
| LLaMA-3 | 8B | 15T | 严重过训练(为推理省钱) |
| LLaMA-3 | 405B | 15T | 约 37 tok/param |

### 4.4 ⌨ 动手点

HuggingFace 上能直接拉到处理好的数据集(`HuggingFaceFW/fineweb`、`HuggingFaceFW/fineweb-edu`、`meta-llama/Meta-Llama-3-1-8B` 的数据卡)。拿 1GB 的小切片,自己跑一遍 tokenize 看看"一段话变成多少 token、英文/中文分别几个 token / 字"。

---

## 5. 分词:把文字变成整数序列

神经网络只吃浮点张量,所以先要把文本变成 `uint16`/`int` 整数。这个过程叫分词。和深度学习其他领域不太一样的地方:**分词是模型的一部分**,换分词器,模型得重训。

### 5.1 为什么不用"按字 / 按词"

- 按字:序列太长(一段话变几百个 token),且把"词"语义拆碎。
- 按词:词表爆炸(几十万上百万),稀有词 OOV(词表外)严重。
- 折中:**子词 subword**:常见词整个成一个 token,稀有词拆成几个子词。

### 5.2 主流算法

- **BPE(Byte-Pair Encoding)**:从字符开始,反复合并最高频的相邻对。GPT-2/3、LLaMA 都用 BPE 的变体。
- **WordPiece**:BERT 用,类似但合并准则略不同。
- **Unigram + SentencePiece**:从大候选里删到最优子集,_sentencepiece_ 包了 BPE/Unigram,常见于多语言模型。
- **BBPE(Byte-level BPE)**:在 **UTF-8 字节**层面而非字符层面做 BPE,天然 OOV 免疫(任何文本都能编出来)。

### 5.3 词表大小的权衡

| 维度 | 小词表 | 大词表 |
|---|---|---|
| 序列长度(同样文本) | 更长 → 更耗算力 | 更短 → 省算力 |
| Embedding/Unembed 参数 | 小(=V·d) | 大 |
| 稀有词 | 拆成多 token | 单独成 token |
| 代表值 | ~32k(GPT-2) | 65k~256k(近年主流) |
| 多语言 | 单语词表小就够 | 多语言混排需要更大 |

⚠ 多语言分词坑:纯英文词表压中文时,一个汉字常拆成多个字节 token,效率极低、且影响学习。所以多语言模型要么扩大词表,要么针对性提升语种覆盖。

### 4→5 衔接:token 之外还有 special tokens

`<bos>`(句首)、`<eos>`(句尾)、`<pad>`(补齐)、可能还有 `<|im_start|>` 这类对话标记。它们也是词表的一员,但语义靠后训练赋予。

---

## 6. 训练目标:为什么"预测下一个 token"能产生智能

这是预训练最反直觉、又最重要的一环。看似只是个"接龙",但它就是 GPT 系的全部训练信号。

### 6.1 目标函数

对一条 token 序列 $x_1,\dots,x_T$,模型在每个位置 `t` 输出分布 $\hat p(x_{t+1}\mid x_{1:t})$,目标是最小化**负对数似然(= 交叉熵)**:

$$
\mathcal{L} = -\sum_{t=1}^{T-1} \log \hat p(x_{t+1}\mid x_{1:t})
$$

等价说法:**模型在解一个每步都对整个词表的 V-类 softmax 分类**。

### 6.2 为什么这么浅的目标能学出"智能"

⚑ **核心直觉**:要**最优地**预测下一个 token,模型被迫对所有能解释当前上下文的世界模型建模。一句话后面接什么是确定的,模型为了逼近 1 的概率,必须"理解"前面的内容——这迫使它学到语法、事实、推理、翻译、代码…… 能力是**为了把下一个 token 预测对**而**副产品似地**学到的,不是被显式教的。

这就是"in-context learning / few-shot"的基础:给几个例子再提问,它能在不更新权重的情况下用例子引导续写。

### 6.3 实现细节(你之前没注意过的几个点)

- **Loss masking**:一条长文本切成训练样本时,只对该预测的位置计 loss,`<pad>` 等不计入(避免模型学着预测填充符)。
- **Pack / Concatenate**:把短样本拼成一个长 block 直到达到 `block_size`(如 4096),用 attention mask 保证块内不串扰,省算力。
- **Causal = 一次 forward 训 T 个位置**:因果模型一次前向里,第 1..t 个位置的预测可以并行计算(共享前缀),所以训练比"逐 token 生成快不了多少"——训练时并行,推理时还得逐 token 自回归。

### 6.4 为什么现在不用 MLM(BERT)式目标

- MLM 只对随机选的 15% token 算 loss,信号密度低、训练效率差。
- BERT 是双向的,无法直接生成(续写)。
- Scaling 实验证实:decoder-only 的自回归目标在 scale 上更优、且统一了生成/对话。所以现代模型统一回归到 NTP。

---

## 7. 训练循环:从一次前向到一次更新

你懂反传,所以这里侧重"预训练特有的超参和 trick"。

### 7.1 一个最小训练骨架(伪代码,可直接对照 nanoGPT)

```python
# 极简自回归预训练循环,仅示意
model = DecoderLM(vocab_size, d_model, n_layers, n_heads).to(device)
opt = AdamW(model.parameters(), lr=peak_lr, betas=(0.9, 0.95), weight_decay=0.1)

for step, batch in enumerate(dl):
    x, y = batch[:, :-1], batch[:, 1:]          # 目标是右移一位
    logits = model(x)                           # [B, T, V]
    loss = F.cross_entropy(
        logits.reshape(-1, V), y.reshape(-1),  # 逐 token 交叉熵
        ignore_index=pad_id                     # 不对 pad 算 loss
    )
    opt.zero_grad(set_to_none=True)
    loss.backward()
    torch.nn.utils.clip_grad_norm_(model.parameters(), 1.0)  # ⚠ 梯度裁剪
    lr = get_lr(step)                            # warmup+cosine 调度
    for pg in opt.param_groups: pg['lr'] = lr
    opt.step()
```

⌨ 这就是 nanoGPT 的核心。单卡、几百行、能在一张 GPU 上训一个 char-level 小 GPT 先建立直觉。

### 7.2 优化器与学习率(LLaMA 家族典型值)

| 项 | 典型值 | 说明 |
|---|---|---|
| 优化器 | **AdamW** | β1=0.9, β2≈0.95, eps=1e-8 |
| 峰值学习率 | 3e-4(小)~ 6e-5(很大) | lr ≈ 随 global batch 线性缩放 |
| Warmup | 训练前 ~0.2%~2% 步数 | 防止初期大 lr 把模型打散(最易发散的窗口) |
| 调度 | **warmup → cosine decay** 到峰值的 ~0.1 | 快结束时还常见再分段降 lr |
| Weight decay | ~0.1 | 一般只作用在权重矩阵、不作用在 norm/bias |
| 梯度裁剪 | grad clip = 1.0 | 防 loss spike 关键 |

⚑ 直觉:warmup + grad clip 是大模型训练不发散的两大保险。大模型前期最不稳定,warmup 用小 lr 跑几千步"扶稳"再放。

### 7.3 Batch:micro / global / gradient accumulation

- **micro batch**:一张卡一次前向能塞的样本数。
- **gradient accumulation**:多次 micro batch 累加梯度再 step,等效增大 batch 而不爆显存。
- **global batch**:所有卡所有累积加起来的等效 batch。

预训练的 global batch 常用 **token 数**而非样本数衡量,如 LLaMA-2 70B 的 batch ≈ 4M token。batch 越大、lr 一般越大,稳态收敛也更好,但太大会损失 sharpness 探索力。

### 7.4 精度与内存

| 精度 | 字节/参数(权重) | 训练用否 | 说明 |
|---|---|---|---|
| fp32 | 4 | 老黄历 | 大模型不再纯用 |
| fp16 | 2 | 需 loss scaling | 易溢出,要动态 loss scale |
| **bf16** | 2 | 现在主流 | 动态范围大、几乎不需要 loss scaling,A100 起原生支持 |
| fp8 | 1 | 前沿 | 训练用 fp8 更激进省内存/加速 |

⚑ 大模型默认 **bf16 + fp32 master 权重**。bf16 动态范围宽,对训练稳定性比 fp16 友好得多——能用就别用 fp16。

### 7.5 训练显存账(为什么 7B 单卡都训不下)

Adam 混合精度下,每参数固定开销约 **16 字节**:

```
fp32 master 权重   4
fp32 Adam momentum  4
fp32 Adam variance  4
bf16 权重          2
bf16 梯度          2
                  ───
                  16 bytes / param
```

那么 7B 仅"模型+优化器"就要 ~112 GB,还要加激活值、激活重计算——所以预训练**根本不可能单卡**,必须分布式(见第 9 章)。这也是为什么"懂深度学习"到"懂预训练"中间隔着一整座工程山。

---

## 8. Scaling Law:整件事的指挥棒

这是预训练里最该吃透的理论工具。它告诉你:**给定预算,模型多大、数据训多少才最优。**

### 8.1 两条曲线和 FLOPs 估算

- 训练总算力 $C \approx 6 \cdot N \cdot D$,其中 $N$=参数,$D$=token 数。(Kaplan 2020 原始 scaling law。)
- 损失随 size、data、compute 都是**幂律**下降:加大投入,loss 一直在降,只是越来越慢。

### 8.2 Kaplan(2020) vs Chinchilla(2022)

- Kaplan:建议"算力多投在模型变大约上",数据相对用得少。
- **Chinchilla(实验更彻底,纠正了 Kaplan)**:compute-optimal 训练下,**模型和数据应按比例放大**,经验值 **≈20 token / 参数**。
- 实操含义:70B 模型,Chinchilla 最优 ≈ 训 1.4T token。

### 8.3 过训练(overtraining):当前实际趋势

Chinchilla 是 **train-compute 最优**(花最少算力达到最低 test loss)。但工程上常关心 **deploy-compute / 推理成本**,后者往往更贵。

→ 把小模型训远多于 Chinchilla 最优的 token,换来**同精度下参数更小、推理更便宜**。例如 LLaMA-3 8B 训 15T token(≈187 tok/param)。

| 目标 | 策略 | 代表 |
|---|---|---|
| 最快训练到目标 loss | Chinchilla 配方 | Chinchilla、多数早期研究 |
| 推理总成本最低 | 过训练小模型 | LLaMA-3 8B |
| 极致能力 | 巨大模型 + 巨量数据 | LLaMA-3 405B、GPT-4 |

⚑ 怎么用:决定做多大模型后,先按 Chinchilla 配 token 量做"训练版"实验;若你的部署里推理远贵于训练,就把小模型过训练。

### 8.4 涌现能力(及争议)

文献称:能力不在小规模线性外推、而在某规模上"陡然冒出",如多步推理、代码、in-context 任务。

⚠ `Are Emergent Abilities a Mirage?`(Schaeffer 2023):很多"涌现"是**评测指标离散化**(如 0/1 exact-match)造成的假象,换成连续 metric 后是平滑提升。读论文时要警惕这点,别把涌现当玄学。

---

## 9. 分布式训练:为什么大模型训练是一门并行计算工程

第 7.5 节已说明 7B 单卡装不下。所以预训练必跑分布式。关键:你要同时切**显存**和**算力**到很多卡上,且尽量少为通信付代价。

### 9.1 四种基础并行(记住:切的是不同的轴)

| 并行 | 切的轴 | 内存省什么 | 通信代价 | 典型搭配 |
|---|---|---|---|---|
| 数据并行 DP | 切 **batch** | 不省(每卡全模型) | all-reduce 梯度 | 万金油 |
| ZeRO / FSDP | 切 **optimizer/grad/param** shard | 大省内存(仍 DP 语义) | 分阶段 all-gather | 取代纯 DP |
| 张量并行 TP | 切 **权重矩阵的行/列** | 省 weights+激活 | 高(NVLink 内) | 同节点内 |
| 流水线并行 PP | 切 **层**(模型分段) | 省一部分 | pipeline 气泡 | 跨节点 |
| 序列并行 SP/CP | 切 **序列长度** | 长上下文必备 | ring/P2P | 长上下文 |

⚑ 直觉:
- **DP/ZeRO/FSDP**:同模型多卡吃不同数据,梯度聚合。是主力。
- **TP**:把一个矩阵乘法拆到几张卡上算,适合 **NVLink 紧耦合的同一台机器内**,跨机做 TP 会被通信拖死。
- **PP**:把模型的第 1~K 层放机器 A、K+1~N 层放机器 B,用 micro-batch 填气泡。
- 实际上是 **3D-parallel = DP × TP × PP**,沿三条空间轴切卡。

### 9.2 ZeRO / FSDP 到底省在哪

普通 DP 每卡都存**完整**的:master 权重 + Adam 状态 + 梯度 + 权重。ZeRO 逐级切分:

| 阶段 | 切掉 | 单卡内存 |
|---|---|---|
| ZeRO-0 / 普通 DP | — | 全量(最大) |
| ZeRO-1 | optimizer states 分片 | ~1/4 of full,明显降 |
| ZeRO-2 | + gradients 分片 | 再降 |
| ZeRO-3 | + parameters 分片(=FSDP 近义) | 最省,但前向/反传要 all-gather 重聚 |

FSDP 就是 PyTorch 原生的 ZeRO-3 风格实现,目前是 HuggingFace/PyTorch 主流。

### 9.3 激活值内存 与 重计算(activation checkpointing)

除了 16 字节/参数,显存大头其实是**激活值**——随序列长度和 batch 线性增长、随层数线性增长。

→ **gradient checkpointing / 激活重计算**:前向不存激活,反传时重算一遍。拿 ~30% 算力换掉一大块显存。预训练几乎必开。

### 9.4 通信:实际并行时的一半时间花在这。

大模型训练里"GPU 非空载比例(MFU)"很难超过 50%,因为大量时间在 all-reduce/all-gather。所以:
- TP 放同节点(NVLink 带宽 ~600GB/s vs 跨机 IB ~50–100GB/s 量级)。
- DP/ZeRO/PP 跨节点。
- 集群拓扑(IB / NVLink / switch)直接决定能多有效扩展。

---

## 10. 稳定性与评估:怎么知道训对了

预训练失败最常见的不是"loss 不降",而是**中途突然 loss 飙升 / 发散 / NaN**。这是预训练特有的炼丹难题。

### 10.1 训练中盯哪些量

- **train loss 曲线**:应平滑下降、末段缓慢收敛;不应有持续上升或突跳。
- **验证 loss / perplexity**:
  - $\text{PPL} = \exp(\text{avg cross-entropy})$,越低越好。
  - ⚠ PPL **跨词表/跨分词器不可比**,只能比同设置下。
- **梯度 norm**:暴涨常是发散前兆。grad clip 已兜底,但仍要监控。
- **各层激活/norm** 不爆炸、不坍缩。深层 pre-norm 模型尤其要观察。

### 10.2 为什么会 spike / 发散,怎么治

| 原因 | 处置 |
|---|---|
| 学习率太大 / warmup 太短 | 加长 warmup、降峰值 lr |
| 数值溢出(fp16) | 换 bf16、必要时回退 fp32 |
| 梯度爆炸 | grad clip = 1.0(已标配) |
| 注意力 logits 爆炸 | **QK-norm**(对 Q·K 做 RMSNorm) |
| 某层异常 | 跳回上一个稳定 checkpoint + 降 lr 重启("loss spike recovery") |

⚑ 实践:大模型训练普遍会偶发 loss spike。常规做法是**用 cosine 末段 lr 降下来**,出现持续发散时**回滚检查点 + 把 lr 调到原来的 1/2 重启**——不是从头训。

### 10.3 Checkpoint:救命稻草

- 周期性存 checkpoint,**关键要存**:权重 + 优化器状态(m,v)+ lr scheduler 位置 + 训练步数。
- ⚠ **只存权重 = 万一要 resume 训练得从头**;只存权重能用于推理,不能用于续训。
- 训练动辄几天~几十天,崩溃恢复是刚需,不是可选项。

### 10.4 评什么(预训练后)

底模评测和 Chat 模型评测不同:

- **底模**:perplexity + **下游 zero-shot / few-shot**:MMLU(知识)、HellaSwag/PIQA(常识)、ARC(推理)、GSM8K/MATH(数学)、HumanEval(代码)。
- **Chat/SFT 后**:MT-Bench、Arena 胜率、人工/AI 偏好对比。
- 工具:`lm-evaluation-harness`、`lighteval`(HF)。

---

## 11. 预训练 vs 后训练:边界在哪(简述)

预训练做出"会续写文本"的底模。要变成"会听指令、不胡说、好用"的助手,还差后训练(post-training / alignment):

| 阶段 | 做什么 | 数据 | 目标 |
|---|------|---|---|
| SFT(监督微调) | 在(指令,回答)对上微调 | 人写/合成的指令数据 | 让模型"会遵循指令"回答 |
| 奖励建模 RM | 从人工偏好对训练打分模型 | (A 好 B 坏) 对 | 学人类偏好 |
| RLHF(PPO) | 用 RM 当奖励,RL 优化策略 | 模型自采样 | 进一步对齐偏好 |
| DPO | 不训 RM,直接从偏好对里学 | 偏好对 | 更稳、更省的一种对齐 |
| RL / RLVR(推理类) | 用可验证奖励(代码/数学对错)做 RL | 可判对错的任务 | 训出会推理的模型(DeepSeek-R1 / o1) |

⚑ 再强调:这些阶段是**塑造**,不是注入能力。模型的基础智力在预训练就已经定下,后训练把它导向好用、可控、安全的形态。这也是本指南主攻预训练而仅留后训练概览的原因。

---

## 12. 实践路径:从小到大亲手跑一遍

把上面所有概念"落地"的最快路径。按顺序做,每步都让你能解释清楚发生了什么。

### 阶段 A:单卡 char-level 小 GPT(1~2 天,最关键)

- 跑 **Karpathy 的 nanoGPT**(仓库:`karpathy/nanoGPT`),用 `shakespeare_char` 任务。
- 目标:理解第 7 章那套训练循环、看 loss 真的下降、能采样出像莎翁的文本。
- ⌨ 重点修改:改 `block_size`、改 `lr`、改 batch,看 loss 和生成质量怎么变。

### 阶段 B:换个真数据 + BPE 分词

- 换成有意义的语料(如 OpenWebText 子集 / 一个你感兴趣的中文语料)。
- 接上真实 **BPE tokenizer**(用 HuggingFace `tokenizers` 或 sentencepiece),感受词表大小的取舍。
- 此时你已经能复现一个"小 GPT-2"。

### 阶段 C:读懂论文级配置

- 对照读 **LLaMA 系列论文** + **Karpathy "Let's build GPT"** 视频,把每个超参对回第 7、8、9 章的表格。
- 试算:给定 8B、15T token 和某卡数,训练大概花多少 FLOPs、多少 GPU·天。

### 阶段 D:上手分布式框架

- `torch.distributed` / HuggingFace `accelerate` / `deepspeed` / `Megatron-LM`。
- 先用 ZeRO-2 在 2~8 张卡上跑 B 阶段的小模型,观察显存和 MFU 变化,对照第 9 章。

### 阶段 E:进阶(按方向深入)

- 数据向:跑一遍 FineWeb-Edu 的处理 / 看一个去重 pipeline,理解第 4 章。
- 算力向:研究 TP+PP 的 Megatron 配方、ring-attention 长上下文。
- 推理/长文向:RoPE 外推、YaRN、KV cache、推测解码。

---

## 13. 自测清单(读完应能回答)

1. 预训练和后训练分别决定模型的什么?为什么?
2. 一个 7B、bf16+Adam 的模型,训练时单看"权重+优化器"大约要多少 GB?为什么单卡训不动?
3. Chinchilla 说一个 70B 模型该训多少 token?LLaMA-3 8B 实际训了多少?这叫什么策略,动机是什么?
4. 预训练损失是什么?为什么"预测下一个 token"能学出知识/推理?
5. warmup + grad clip 各自防的是什么问题?
6. data parallel / ZeRO / TP / PP 分别切的是哪个轴?为什么 TP 不要跨节点?
7. 训练 loss 突然飙升,标准处置流程是什么?
8. PPL 为什么不能跨分词器比较?

能答上来,你就比大多数"只跑过 SFT"的人更懂预训练了。

---

## 14. 资源清单(都是真实可查)

### 视频 / 教程
- **Karpathy《Let's build GPT》** + **《Neural Networks: Zero to Hero》**(单卡从零搭 GPT,实践首选)
- **Karpathy《Deep Dive into LLMs》**(数小时系统讲解从预训练到后训练全流程)
- **Jay Alammar《The Illustrated Transformer》/《The Illustrated GPT-2》**(直觉图解 Transformer)
- **Hung-yi Lee 台大 ML 课程**(中文,讲解 Transformer/语言模型)

### 必读论文(按顺序)
1. `Attention Is All You Need`(Vaswani 2017)— 架构地基
2. GPT-2 / GPT-3 论文 — decoder-only + scaling 兴起
3. `Training Compute-Optimal Large Language Models`(Hoffmann 2022,即 **Chinchilla**)— Scaling Law 校正
4. `Llama / Llama-2 / Llama-3` 技术报告 — 现代主流配方的"说明书"
5. `DeepSeek-V3 / R1` 技术报告 — 当前低成本高效训练 + 推理型 RL 的前沿做法
6. `Are Emergent Abilities a Mirage?`(Schaeffer 2023)— 对涌现的批判视角

### 仓库 / 工具
- `karpathy/nanoGPT` — 几百行复现训练循环
- `HuggingFaceFW/fineweb`、`fineweb-edu` — 处理好的预训练数据切片
- HuggingFace `tokenizers` / `transformers` / `accelerate`
- `deepspeed`(ZeRO 系列)、`Megatron-LM`(TP + PP 主力)、PyTorch FSDP
- `lm-evaluation-harness`、`lighteval` — 评测

### 代码 / 数据集
- FineWeb / FineWeb-Edu(RefinedWeb 风格网页数据)
- RedPajama、DCLM(DataComp for Language Models)
- The Stack / GitHub(代码语料)
- Common Crawl(网页数据的源头)

---

## 15. 术语表(中英对照)

| 英文 | 中文 | 一句话 |
|---|---|---|
| Pretraining | 预训练 | 在海量无标注文本上自监督训练底模 |
| Post-training / Alignment | 后训练/对齐 | 把底模驯成好用助手的阶段 |
| Next Token Prediction / Causal LM | 下一 token 预测/自回归 | 预训练目标 |
| Cross-entropy | 交叉熵 | 预训练损失 |
| Perplexity (PPL) | 困惑度 | exp(avg loss),越低越好 |
| Tokenization / Tokenizer | 分词 | 文本→整数序列 |
| BPE / SentencePiece | 子词分词算法 | 主流分词器 |
| Decoder-only / Causal mask | 纯解码器/因果掩码 | 只允许看前文 |
| Scaling Law | 缩放律 | loss 随 size/data/compute 幂律下降 |
| Chinchilla | Chinchilla 法则 | ~20 token/参数的最优训练量 |
| Overtraining | 过训练 | 远超 Chinchilla 训小模型,省推理 |
| Data / Tensor / Pipeline Parallel | 数据/张量/流水并行 | 切不同轴的并行 |
| ZeRO / FSDP | 分片并行 | 切 optimizer/grad/param 省内存 |
| Gradient Accumulation | 梯度累积 | 多次 micro batch 等效大 batch |
| Activation Checkpointing | 激活重计算 | 以算力换激活显存 |
| MFU | 模型算力利用率 | GPU 真在做有效计算的比例 |
| Loss spike | loss 飙升 | 大模型训练常见的发散征兆 |
| Warmup / Cosine decay | 预热/余弦衰减 | 学习率调度,稳训练关键 |
| SwiGLU / RoPE / RMSNorm / GQA | 现代 decoder 组件 | LLaMA 风格架构替换 |
| SFT / RLHF / DPO / GRPO | 监督微调/人类反馈RL/直接偏好优化/组相对策略优化 | 后训练对齐方法 |
| In-context learning | 上下文学习 | 不更新权重、靠例子引导续写 |
| KV cache | KV 缓存 | 推理时缓存已算 k/v 加速自回归 |

---

## 16. 一页纸总结

> **预训练 = 在海量无标注文本上,用"预测下一个 token"的自监督目标,把一个 decoder-only Transformer 训成通用语言底模。** 它烧掉 99% 的算力、决定模型智力上限;后训练只是塑造。难点不在"前向反传"(你会),而在四座工程山:**数据质量**(去重+过滤+配比)、**Scaling Law 配比**(模型多大/训多少 token)、**分布式训练**(DP/ZeRO/TP/PP 显存与通信)、**训练稳定性**(warmup+grad clip+QK-norm+回滚 checkpoint)。从 nanoGPT 开始亲手跑一个 char-level 小 GPT,是把这些概念从抽象变成手感的最快路径。

---

*建议把这份文档保存在仓库里,每读完一节对照改一次你的小模型实验。理论不碰代码就成了背诵,代码没理论就成了玄学。两边一起走,最快。*